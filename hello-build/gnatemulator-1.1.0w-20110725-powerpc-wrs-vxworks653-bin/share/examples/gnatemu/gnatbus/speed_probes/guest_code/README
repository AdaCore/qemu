Guest code for GNATbus speed_probe example
******************************************

This example is available on leon-elf, leon3-elf, erc32-elf and ppc-elf.

1) Compiling the example.

   To compile, simply invoke gnatmake:

   <target>-gnatmake -Pspeed_computation.gpr  -XTARGET=<target>

2) Running the example.

   <target>-gnatemu computation_loop -eargs --gnatbus localhost:8032
